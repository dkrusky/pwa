(function(){
    'use strict';

    angular
        .module('WebsiteApp')
        .config(config);

    /*@ngInject*/
    /**
     * Configures some generic app stuff.
     * @returns {undefined}
     */
    function config(){ }
})();