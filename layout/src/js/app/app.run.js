(function(){
    'use strict';

    angular
        .module('WebsiteApp')
        .run(runBlock);

    /*@ngInject*/
    /**
     * Basic initialization for the app.
     * @returns {undefined}
     */
    function runBlock($rootScope, $anchorScroll, $state){ }
})();